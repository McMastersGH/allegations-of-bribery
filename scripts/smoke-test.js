// smoke-test.js removed during repository cleanup
// The original smoke test implementation was removed to prepare the repository for upload.
// If you want to reintroduce smoke tests later, add test scripts under `scripts/` and
// restore entries in `package.json` as needed.
