// Badge/count feature removed — this module is a no-op stub to avoid runtime errors
export async function populateNewPostsCount() {
  return Promise.resolve();
}

export function startNewPostsCount() {
  // no-op
}
